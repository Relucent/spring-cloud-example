
# 需要从Git上读取的资源名称(不带yml后缀)
spring.cloud.config.name=config_client

# 本次访问的配置项
spring.cloud.config.profile=dev

# 标签
spring.cloud.config.label=master

# 配置服务
spring.cloud.config.uri=http://config9002.yyl:9002
